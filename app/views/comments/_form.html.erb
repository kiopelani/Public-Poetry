<%= render 'shared/form_errors', :object => comment %>
<%= form_for [poem, comment], remote: true do |f| %>
  <%= f.label :comment %><br />
  <%= f.text_area :content %><br />
  <%= f.submit %>
<% end %>
